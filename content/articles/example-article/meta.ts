/**
 * Метаданные тестовой статьи (Lorem Ipsum).
 *
 * `readingTime` НЕ указывается вручную — он рассчитывается автоматически
 * из содержимого `article.md` на этапе сборки (см. `lib/articles`).
 *
 * `cover` НЕ указывается — обложка читается как `cover.webp` из каталога
 * статьи и становится автономной частью сущности (см. `lib/articles`).
 */
export default {
    slug: 'example-article',
    title: 'Пример инженерной статьи',
    description:
        'Тестовая статья для проверки системы публикации материалов: заголовки, списки, цитаты, изображения и автоматический расчёт времени чтения.',
    category: 'Пожарная безопасность',
    publishedAt: '2026-08-14',
    status: 'published',
};
